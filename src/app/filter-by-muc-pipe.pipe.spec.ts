import { FilterByMucPipePipe } from './filter-by-muc-pipe.pipe';

describe('FilterByMucPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByMucPipePipe();
    expect(pipe).toBeTruthy();
  });
});
