import { FilterByMuc2Pipe } from './filter-by-muc2.pipe';

describe('FilterByMuc2Pipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByMuc2Pipe();
    expect(pipe).toBeTruthy();
  });
});
